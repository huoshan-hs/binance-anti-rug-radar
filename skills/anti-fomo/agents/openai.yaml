interface:
  display_name: "Anti-FOMO Binance Risk"
  short_description: "Detect chain, analyze token risk, discover hot tokens, and monitor anomalies."
  default_prompt: "Use $anti-fomo to analyze a token contract or discover hot/anomalous on-chain tokens with a concise Chinese report."

policy:
  allow_implicit_invocation: true
